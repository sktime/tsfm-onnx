# Ignore-list for git (nothing here is deleted -- these entries only keep
# big/generated things out of version control if this becomes a git repo).
.venv/
.venv-export/
.venv-toto/
__pycache__/
onnx/*.onnx
tests/node_modules/
tests/vector.json
tests/package*.json
